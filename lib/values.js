'use strict';

// this module

const CONFIG_FILE = '.blinkmrc.json';

const MAX_REQUESTS = 5;

const SCOPE_EXAMPLE = 'https://service.domain/project';

module.exports = {
  CONFIG_FILE,
  MAX_REQUESTS,
  SCOPE_EXAMPLE
};
